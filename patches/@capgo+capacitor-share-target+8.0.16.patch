diff --git a/node_modules/@capgo/capacitor-share-target/android/src/main/java/app/capgo/sharetarget/CapacitorShareTargetPlugin.java b/node_modules/@capgo/capacitor-share-target/android/src/main/java/app/capgo/sharetarget/CapacitorShareTargetPlugin.java
index d734dcc..b4f5c42 100644
--- a/node_modules/@capgo/capacitor-share-target/android/src/main/java/app/capgo/sharetarget/CapacitorShareTargetPlugin.java
+++ b/node_modules/@capgo/capacitor-share-target/android/src/main/java/app/capgo/sharetarget/CapacitorShareTargetPlugin.java
@@ -85,8 +85,11 @@ public class CapacitorShareTargetPlugin extends Plugin {
                 }
                 shareData.put("files", files);
 
-                // Notify listeners
-                notifyListeners("shareReceived", shareData);
+                // Notify listeners. retainUntilConsumed=true: en un arranque en
+                // frio por "Compartir", este load() se ejecuta antes de que el
+                // JS registre el listener (la pagina aun se esta cargando), y
+                // sin retencion Capacitor descarta el evento para siempre.
+                notifyListeners("shareReceived", shareData, true);
                 Log.d(TAG, "Share received: " + shareData.toString());
             } catch (Exception e) {
                 Log.e(TAG, "Error handling shared content", e);
