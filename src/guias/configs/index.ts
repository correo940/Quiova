import type { GuiaBundle } from '../engine/types'

// ── Register new guides here ───────────────────────────────────────────────────
// One import per guide. The bundle includes config + any custom components.
// The engine needs no changes when a new guide is added.
//
// Example:
//   import { ciclosCircadianosBundle } from './ciclos-circadianos'
//   'ciclos-circadianos': ciclosCircadianosBundle,

export const guides: Record<string, GuiaBundle> = {
  // guides go here
}
