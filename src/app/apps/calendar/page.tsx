import CalendarClient from '@/components/apps/calendar/calendar-client';
import React from 'react';

export default function CalendarPage() {
  return (
    <div className="w-full">
      <CalendarClient />
    </div>
  );
}

